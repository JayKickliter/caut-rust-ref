mod rust_test {
    #[derive(Debug)]
    pub enum Someenum {
        // fields
    }

    #[derive(Debug)]
    pub enum FieldEnum {
        // fields
    }

    #[derive(Debug)]
    pub struct Crecord {
        // fields
    }

    #[derive(Debug)]
    pub struct Brecord {
        // fields
    }

    #[derive(Debug)]
    pub struct Arecord {
        // fields
    }
}