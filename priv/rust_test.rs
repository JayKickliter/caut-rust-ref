mod rust_test {
    #[derive(Debug)]
    pub struct Unsigned8(type);

    #[derive(Debug)]
    pub enum Someenum {
        // fields
    }

    #[derive(Debug)]
    pub enum Primitivetest {
        // fields
    }

    #[derive(Debug)]
    pub struct Number64(type);

    #[derive(Debug)]
    pub enum FieldEnum {
        // fields
    }

    #[derive(Debug)]
    pub struct Crecord {
        // fields
    }

    #[derive(Debug)]
    pub struct Brecord {
        // fields
    }

    #[derive(Debug)]
    pub struct Arecord {
        // fields
    }

    #[derive(Debug)]
    pub enum AUnion {
        // fields
    }
}